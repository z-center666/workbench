services:
  - type: web
    name: workbench
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python server.py"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.3"
    healthCheckPath: /api/health
    autoDeploy: true
